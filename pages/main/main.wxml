<view class='content'>
  <image class='background' src="{{imagesrc}}" mode="aspectFill"></image> 
  <import src="../template/template.wxml"/>
  <view class="transTop">.</view>
  <view wx:if="{{platformType=='android'&&!resultStatus}}" class="weui-btn_cell weui-btn_cell-primary"
style="background-color:rgba(255,255,255,0.7); color: darkgreen;" bindtap="homePage">回到首页</view>
  <view>
    <template is="question-list" data="{{questions, stage, percentage, delayTime}}"/>
  </view>
  <view class="central-subpage" wx:if="{{resultStatus}}" style="margin-top: 240rpx; padding: 50rpx;">  
    <i class="weui-icon-success weui-icon_msg"s></i>
    <view class="icon-box__ctn">
      <h3 class="icon-box__title">已完成</h3>
      <view class="icon-box__desc">你已完成全部测试</view>
    </view>
    <text>\n</text>	
    <button open-type="getUserInfo" bindgetuserinfo="getUserInfo" class="weui-btn weui-btn_primary">查看结果</button>
  </view>
  
</view>
